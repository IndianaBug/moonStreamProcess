C:\coding\SatoshiVaultEnv\api_stream\Scripts\activate

cd C:\coding\TradeStreamEngine


!git clone https://github.com/CSchoel/nolds.git
cd nolds
python setup.py install


# binanceStreams aggTrade to ismply trade

import nolds
from scipy.stats import skew, kurtosis

self.snapshotO["entropyBuy"] = nolds.sampen(self.snapshot_total.copy().drop(["price"], axis=1).sum(axis=1).values)
self.snapshotO["skewness"] = skew(self.snapshot_total.copy().drop(["price"], axis=1).sum(axis=1))
self.snapshotO["kurtosis"] = kurtosis(self.snapshot_total.copy().drop(["price"], axis=1).sum(axis=1))


calculate skewness, entropy kurtosis and other interesting metrics and shit